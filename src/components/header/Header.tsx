import { HeaderSearchBar } from "./HeaderSearchBar"

export const Header = () => {
    return (
        <header className="ml-28 md:ml-32 max-w-lg">
            <HeaderSearchBar />
        </header>
    )
}