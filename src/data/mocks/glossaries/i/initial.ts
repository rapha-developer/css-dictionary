import { GlossaryTermProps } from "../../../../types/GlossaryProps";

export const initial: GlossaryTermProps = {
    name: "initial",
    definition: "A palavra-chave initial é usada para definir uma propriedade CSS para seu valor padrão. A palavra-chave initial pode ser usada para qualquer propriedade CSS e em qualquer elemento HTML.",
    references: []
}