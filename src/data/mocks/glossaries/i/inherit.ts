import { GlossaryTermProps } from "../../../../types/GlossaryProps";

export const inherit: GlossaryTermProps = {
    name: "inherit",
    definition: "A palavra-chave inherit especifica que uma propriedade deve herdar seu valor de seu elemento pai. A palavra-chave inherit pode ser usada para qualquer propriedade CSS e em qualquer elemento HTML.",
    references: []
}